# Hard pins enforced machine-wide via the PIP_CONSTRAINT environment variable.
# Any `pip install` (by any tool) must respect these — this is the permanent
# fix for the recurring sse-starlette upgrade breaking FastAPI 0.111 startup
# (TypeError: Router.__init__() got an unexpected keyword argument 'on_startup').
starlette==0.37.2
fastapi==0.111.0
